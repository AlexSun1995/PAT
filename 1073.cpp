/* 1073. Scientific Notation (20) *//* starting at 2016-9-3 8:56 AM*//* fisrt submit: 9:30 14/20 *//* Accepted! 20/20 9:42 */#include<iostream>#include<string>#include<sstream>using namespace std;int main(){	freopen("D:\\in.txt","r",stdin);	string str;	cin>>str;	/* if num,exponent are positive,flag is true */	bool flag1,flag2;	flag1=flag2=true;	string snum,sexp;	if(str[0]=='-') flag1=false;	str.erase(2,1);   // erase a character,	for(int i=1;i<str.size();i++)	{   		if(str[i]=='E')		{			snum=str.substr(1,i-1);			if(str[i+1]=='-') flag2=false;			sexp=str.substr(i+2,str.size()-i-2);		}	}	if(!flag1) cout<<"-";	stringstream ss(sexp);	int exp;	ss>>exp;	if(!flag2)	{		if(exp>=1)		{			cout<<"0.";			exp--;		}		while(exp>0)		{			cout<<"0";			exp--;		}		cout<<snum;	}	else if(flag2)	{		if(exp>=snum.size()-1)		{			cout<<snum;			exp-=snum.size();			exp++;		}		else if(exp<snum.size()-1)		{			cout<<snum[0];			for(int i=1;i<snum.size();i++)			{				if(exp>0){					//cout<<snum[i];					exp--;				}				else if(exp==0)				{					cout<<".";					exp--;				}				 cout<<snum[i];			}						}		while(exp>0){			cout<<"0";			exp--;		}	}	return 0;}